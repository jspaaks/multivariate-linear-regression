#ifndef UNSTANDARDIZED_H
#define UNSTANDARDIZED_H
#include <stddef.h>

void run_unstandardized(size_t nsamples, size_t nfeatures, size_t nepochs, float learning_rate, const char * features_path, const char * labels_path);

#endif
